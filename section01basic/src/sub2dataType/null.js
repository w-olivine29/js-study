// Null (존재하지 않거나 알 수 없는 값)
// 다른 언어와는 다르게 오직 null값만을 포함하는 하나의 자료형으로 사용

let nullValue = null;

console.log(nullValue);
console.log(typeof nullValue); //object

// 변수가 null임을 확인하기 위해서는 typeof 연산자 대신 === 연산자 사용
console.log(nullValue === null); //true
